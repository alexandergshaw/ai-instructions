# JavaScript and TypeScript Rules

- Prefer TypeScript when the repository already uses it.
- Preserve the existing module system and runtime conventions.
- Avoid `any` unless a narrower type is impractical and the tradeoff is justified.
- Do not introduce state-management or framework dependencies unless they materially improve the solution.
- Use the repository's established formatter, linter, and test runner when present.
