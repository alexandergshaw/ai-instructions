# Java Rules

- Preserve the existing package structure and naming conventions.
- Follow the repository's build tool conventions rather than introducing a new build path.
- Avoid unnecessary inheritance when composition or small focused classes are sufficient.
- Prefer explicit dependency boundaries and constructor-based wiring when practical.
- Keep public APIs stable unless requirements explicitly call for change.
