# SQL Rules

- Write readable SQL with clear structure and consistent formatting.
- Avoid destructive statements unless the task explicitly requires them.
- Use deterministic ordering whenever result ordering matters.
- Preserve schema assumptions and verify them before changing query behavior.
- Favor parameterized application queries over constructed SQL strings.
