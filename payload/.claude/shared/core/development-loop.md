# Development Loop

The staged development process for this repository lives in the `shared-development-loop` skill,
which is distributed to every repository this system reaches and discovered without needing to be
imported. Use it when a change spans several files or several agents.

The rules bounding what may be done without being asked are in the `shared-agent-floor` skill. This
line is a pointer and restates none of them.
